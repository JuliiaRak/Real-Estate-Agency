package com.solvd.service.validators.number;

import com.solvd.service.validators.Validator;

class BasicIntegerValidator implements Validator<Integer> {

    @Override
    public void validate(String fieldName, Integer entity) {
    }
}
