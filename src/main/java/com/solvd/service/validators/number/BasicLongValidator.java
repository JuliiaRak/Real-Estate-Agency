package com.solvd.service.validators.number;

import com.solvd.service.validators.Validator;

class BasicLongValidator implements Validator<Long> {
    @Override
    public void validate(String fieldName, Long entity) {
    }
}
